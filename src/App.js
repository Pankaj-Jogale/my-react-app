import { useState } from "react";

function App() {
  //passing event parameter from btn

  return (
    <div>
      <input type="button" value="clickme" onClick={} />
    </div>
  );
}
export default App;
